<html>
    <head>
        <script language="javascript" type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.4.4/jquery.min.js"></script>
        <script language="javascript" type="text/javascript" src="arbor/lib/arbor.js" ></script>
        <script language="javascript" type="text/javascript" src="arbor/lib/graphics.js" ></script>
        <script language="javascript" type="text/javascript" src="arbor/lib/renderer.js" ></script>
    </head>
    <body>
      <canvas id="viewport" width="800" height="600"></canvas>
      <script language="javascript" type="text/javascript">
            var sys = arbor.ParticleSystem(1000, 400,1);
            sys.parameters({gravity:true});
            sys.renderer = Renderer("#viewport") ;
            var data = {
                nodes:{
                    {% for nodename, label in nodes %}
                        {{nodename}}:{'label':'{{label}}'},
                    {% endfor %}
                    },
             };
                sys.graft(data);
                {% for a,b in edges %}
                sys.addEdge({{a}},{{b}});
                {% endfor %}
      </script>
    </body>
</html>
